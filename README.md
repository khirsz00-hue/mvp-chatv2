# ZenON – ADHD Assistant (fixed)
Next.js 14 + Supabase + OpenAI + Todoist. Client/server split for assistants (no `node:fs` in client).

## Env
See `.env.example`. Set the same values in Vercel.

## Run
npm i
npm run dev
